@extends('layouts.guest')
@section('title', 'Hyper Forgot Password | Reset Your IT Solutions Account Password')
@section('og-title', 'Hyper Forgot Password | Reset Your IT Solutions Account Password')
@section('meta-desc', 'Reset your Hyper account password securely to regain access to your IT solutions dashboard, services, and project management tools.')
@section('og-desc', 'Reset your Hyper account password securely to regain access to your IT solutions dashboard, services, and project management tools.')
@section('og-type', 'website')
@section('meta-keywords', 'Hyper forgot password, reset IT account password, recover software development account, cloud solutions password, cybersecurity account recovery, Hyper IT services login help')
@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Reset Password') }}</div>

                    <div class="card-body">
                        @if (session('status'))
                            <div class="alert alert-success" role="alert">
                                {{ session('status') }}
                            </div>
                        @endif

                        <form method="POST" action="{{ route('password.email') }}">
                            @csrf

                            <div class="row mb-3">
                                <label for="email"
                                    class="col-md-4 col-form-label text-md-end">{{ __('Email Address') }}</label>

                                <div class="col-md-6">
                                    <input id="email" type="email"
                                        class="form-control @error('email') is-invalid @enderror" name="email"
                                        value="{{ old('email') }}" required autocomplete="email" autofocus>

                                    @error('email')
                                        <span class="invalid-feedback" role="alert">
                                            <strong>{{ $message }}</strong>
                                        </span>
                                    @enderror
                                </div>
                            </div>

                            <div class="row mb-0">
                                <div class="col-md-6 offset-md-4">
                                    <button type="submit" class="btn btn-primary">
                                        {{ __('Send Password Reset Link') }}
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
